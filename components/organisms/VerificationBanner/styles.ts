import { StyleSheet } from "react-native";

export const verificationBannerStyles = StyleSheet.create({
  container: {
    maxWidth: "100%",
    zIndex: 11,
    display: "flex",
    flexDirection: "row",
  },
});
