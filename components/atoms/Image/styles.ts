import { Spacing } from "@/styles";
import { StyleSheet } from "react-native";

export const imageStyles = StyleSheet.create({
  "profile-picture": {
    borderRadius: "50%",
  },
  "company-logo": { borderRadius: "20%" },
  sm: { height: 60 },
  md: { height: 90 },
  lg: { height: 120 },
});
